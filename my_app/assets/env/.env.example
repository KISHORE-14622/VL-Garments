# Flutter App Environment Configuration
# Copy this file to .env and update the values

# Backend API URL - Make sure the port matches your backend server
# If backend shows "API listening on port 3000", use:
API_URL=http://localhost:3000/api

# If backend shows "API listening on port 5000", use:
# API_URL=http://localhost:5000/api
